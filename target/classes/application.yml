server:
  port: 8000
spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/learningEnglish?allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
    username: root
    password: mysql1234
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
        format_sql: true
    hibernate:
      ddl-auto: update
  sql:
    init:
      mode: always
